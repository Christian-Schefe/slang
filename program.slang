let x = 5;
let y = "hi";
let z = true; # this is a comment

fn func(a, b) ();
let w = func;
print(x, y, z, w);

fn hello(x, y, z) {
    let an = x + y+ -z;
    return an;
};

print(hello("a", "b", "c"))
